'use client';
import AdminMarketplaceDashboard from '../page';

/**
 * The /admin/marketplace/dashboard route re-exports the main marketplace page
 * which already has full region-aware KPIs, filtered tables, and dynamic currency.
 */
export default AdminMarketplaceDashboard;
