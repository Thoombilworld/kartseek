/// KARTSEEK — Taxi Module SEO Metadata (Server-side only)
/// This file provides metadata for the taxi module since the layout is 'use client'.

import { moduleMeta } from '@/lib/seo/metadata';

// ── SEO: Dynamic Metadata ───────────────────────────────────────────────────
export const metadata = moduleMeta('taxi');
