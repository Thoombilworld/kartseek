# Plan A Execution Record — Admin Authorization & Regional Isolation

Date: 2026-09-11 · Branch: `feat/admin-platform-upgrade` (from `fix/system-check-2026-09-06` @ 3e6be86) · Mode: subagent-driven development · Plan: `2026-09-11-admin-authz-isolation-plan.md`

This is the controller's ledger, verbatim, kept as the audit trail of how each task was dispatched, reviewed, fixed and ruled on. Task reports and review packages lived in the git-ignored SDD workspace and are not preserved; the commits are.

```text
# SDD ledger — plan: docs/superpowers/plans/2026-09-11-admin-authz-isolation-plan.md
branch: feat/admin-platform-upgrade (from fix/system-check-2026-09-06 @ 9e310a3 + docs commit)
mode: subagent-driven; kickoff docs/superpowers/plans/2026-09-11-plan-a-kickoff.md
baseline: apps/api vitest 37 files / 351 tests green (2026-09-11 11:38); nest build --all baseline running
Task 1 (A1): BASE e79c361, brief task-1-brief.md (108 lines), implementer haiku
baseline: nest build --all exit 0 (2026-09-11)
Task 1 (A1): implementer DONE, commit 6f53041, HEAD 6f53041, review package generated, reviewer sonnet
Task 1 (A1): review — spec ❌ 1 Important: commit 6f53041 trailer says 'Claude Haiku 4.5', plan mandates 'Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>'
Task 1 (A1): minor (deferred): ddos-admin.controller.ts carries Prettier reflow of ~9 @ApiOperation blocks from the lint-staged hook (mechanical, no behaviour change)
dispatch note: module backends already import @app/common; each has vitest.config.mts — run backend specs from modules/<m>/backend with 'npx vitest run src/<path>'; apps/api suite = 37 files (libs/common specs included)
Task 1 (A1): fix round 1/5 dispatched (resume implementer a86be780ff81e1ead: amend trailer)
Task 1 (A1): fix round 1/5 — implementer DONE: amended to 7f749ae (trailer fixed, tree identical); scoped re-review haiku
Task 1 (A1): complete (commits e79c361..7f749ae, review clean after fix round 1; live probe customer 403 / admin 200 → Tested)
Task 2 (A2): BASE 05045e8, brief task-2-brief.md (261 lines), implementer sonnet
Task 2 (A2): NEEDS_CONTEXT — grep matched apps/api/libs/region (separate @app/region lib, 12 importers, same file/class names); ruling: unrelated, delete only the three named files; implementer resumed
A3 pre-dispatch rulings: (1) getDashboardStats(scope?) — users query adds 'AND country = $2', orders query adds 'AND region_code = $2' when scope set; cache key admin:dashboard:stats:<SCOPE|ALL>; (2) getRevenueReport reads global per-day Redis counters (admin:counter:revenue:<date>) with no market dimension → when scope OR country is set throw NotImplementedException('Revenue is not tracked per market yet; Plan C1 routes it to order-service'), global admin unchanged; (3) nothing writes admin:kyc:pending:* in this build — queue empty; filter + NotFound on approve/reject are correct, live probe shows empty list
Task 2 (A2): implementer DONE, commits 178cdea + 7bac5ac, HEAD 7bac5ac, reviewer sonnet
Task 2 (A2): review ✅ approved; ⚠️ trailer resolved by controller (git log shows Claude Fable 5.1 on 178cdea and 7bac5ac)
Task 2 (A2): minor (deferred): market-scope.ts:324 '?? undefined' is a no-op; commit split in two (cleaner, lint-staged scopes); no spec pins a lower-case scope value
Task 2 (A2): complete (commits 05045e8..7bac5ac, review clean; no live probe listed → Tested)
Task 3 (A3): BASE ebd81f9, brief task-3-brief.md (513 lines), implementer sonnet
Task 3 (A3): implementer DONE_WITH_CONCERNS, commit 2b7e674; live probe QA admin 403/200/200/501/200, global admin sees platform totals (48 users / 52 orders)
Task 3 (A3): rulings — (1) admin.service.spec.ts edits accepted (KYC decisions on a non-pending record now 404, intended); (2) async handlers accepted; (3) deferred minor: RPC forwarding masks the 501 message body (status correct; cross-cutting, final review); (4) Redis-fallback users list unscoped → fix sent to implementer before review
A4 pre-dispatch facts: gateway ctor order = redis, kafka, marketplaceClient, userRepo, commissionClient, payoutClient, walletClient, loyaltyClient, orderClient, refundClient (matches brief); Seller is @Entity('sellers') with no schema in the decorator → join via the entity class (innerJoin(Seller, 's', 's.id::text = p.seller_id')) never a bare table string (public.* decoys); Product.seller_id (snake, string|null) and Product.approval_status; Seller.regionCode ↔ column region_code (catalog.getSellersForAdmin uses 's.region_code'); backend payloads: admin_get_seller_by_id / admin_get_product_by_id take a bare string id; approve/reject/suspend/reactivate/block seller take data.{id|sellerId, adminId}; MarketplaceService props sellerRepo/productRepo/kafka/logger; approveProduct must look up the seller via product.seller_id; admin_get_product_by_id handler should return { ...product, sellerRegionCode } so the gateway assert has a field
Task 3 (A3): fix before review DONE (58e51af, Redis fallback scoped); HEAD 58e51af; reviewer sonnet
Task 3 (A3): review ✅ approved; ⚠️ trailer resolved by controller (both commits carry Claude Fable 5.1)
Task 3 (A3): minor (deferred): msgAddAuditLog payload type omits country; approveKyc/rejectKyc re-derive the redis key; gateway spec lacks dashboard()/addAuditLog() cases; revenue rejection spec covers scope only, not country-only; RPC layer masks the 501 message body
Task 3 (A3): complete (commits ebd81f9..58e51af, review clean; live probe QA admin 403/200/200/501/200 → Tested)
Task 4 (A4): BASE af58211, brief task-4-brief.md (446 lines), implementer opus
Task 4 (A4): implementer DONE, commits 065d131 + db2e850, HEAD db2e850; live probe QA admin sees [QA] sellers only, 403 on IN seller read+approve, taxonomy write 403/read 200, products page 2/limit 5; reviewer opus
Task 4 (A4): notes — products.seller_id is uuid (brief's text cast wrong; both sides cast; Product entity comment misleading, out of scope); GET reviews has no admin_get_reviews pattern (reads products; Plan C); scope is forwarded to refund/payout/wallet/loyalty/order/commission services but those handlers ignore it → gateway-side enforcement only until Plan C1
Task 4 (A4): review ❌ 5 Important — (1) inner join hides seller-less products (→ leftJoin); (2) double ::text cast on uuid columns (→ s.id = p.seller_id); (3) wallet/loyalty writes fail open for locked admins (→ refuse inline); (4) dashboard/analytics handlers drop scope (→ scope dashboard for real, analytics fail-closed until C1); (5) four handlers still answer outage with an empty ledger (→ ServiceUnavailableException). Fix round 1/5 dispatched to implementer adb05e4c139ac0b65; re-review sonnet
Task 4 (A4): minor (deferred): three inline actor fallbacks to 'admin' remain (createFlashDeal/approveNomination/rejectNomination); @GlobalEntity on system-health and brand-center under the widened docstring (ratified by plan owner: A8 allowlists system-health); comment rot re seller_id text vs uuid; getPendingSellers(scope?) has no scoped caller (module HTTP surface is closed by HttpSurfaceGuard); invalidateHomeCache message says 'all regions' for a locked admin; 25 discarded this.scopeOf() results read as dead code; getSellers reads result.total without a null guard
A5 pre-dispatch facts: taxi.controller injects svc(TaxiService), vendors, onboarding, config, payouts; handlers today: getVendors(d), getDrivers(d), getPendingDocuments(d) [accepts countryCode but applies NO predicate — documents have no country column, only ownerType+ownerId], reviewDocument(documentId, adminId, decision, rejectionReason?) positional, suspendDriver/blockDriver(driverId, reason), getRateCards(cc), upsertRateCard(cc, vehicleType, rest), getAllConfigs(), getCountryConfig(cc), upsertCountryConfig(cc, rest), getAllPayouts(filters incl. countryCode), processPayouts(payoutIds), getPlatformPayoutSummary({countryCode,startDate,endDate}), svc.getNearbyDrivers(lat,lng,radiusKm,vehicleType) [Redis georadius + driver:profile:<id> JSON], svc.getSurgeMultiplier(lat,lng) [no market; leave for Plan D]. Onboarding service has driverRepo/documentRepo/configRepo only → owner lookup for vendor docs via this.driverRepo.manager.getRepository(TaxiVendorEntity). Pending-docs predicate: leftJoin TaxiDriverEntity/TaxiVendorEntity on ownerType+ownerId and COALESCE(drv.countryCode, ven.countryCode) = :cc. Payout record has countryCode. Taxi specs run from modules/taxi/backend (vitest, existing src/__tests__/taxi.service.spec.ts); build 'nest build'.
Task 4 (A4): fix round 1/5 — implementer DONE: 1e57755 (gateway) + cded9dd (module); live: QA dashboard 7 sellers/9 orders/14 pending vs global 23/26/41; QA 403 on balance writes and all analytics; extended finding 5 to updateCommissionRateCard/setCommissionOverride/removeCommissionOverride/getPayoutStats; scoped re-review sonnet
Task 4 (A4): fix round 1/5 re-review — all 5 addressed, no new Critical/Important
Task 4 (A4): parked — re-reviewer minor 'dead this.scopeOf() in getUserLoyalty' — ruling: keep it; A8's regression spec detects scope by that literal in the route block (refuseUnattributableBalance hides marketScopeOf behind a helper name the spec does not match)
Task 4 (A4): minor (deferred): sendToCommission/sendToPayout collapse any rejection to null → now 503 for a legitimate 4xx from those services (pre-existing); qa-moderation ignores scope; seller list 'search' accepted and dropped by the backend; Plan C1 list in task-4-report.md (ten analytics handlers + wallet/loyalty/refund/payout/order/commission services must enforce scope)
Task 4 (A4): complete (commits af58211..cded9dd, review clean after fix round 1; live probe QA admin [QA]-only sellers, 403 on IN seller read/approve, dashboard 7/9/14 vs 23/26/41 → Tested)
Task 5 (A5): BASE 58a0bdb, brief task-5-brief.md (441 lines), implementer sonnet
Task 5 (A5): implementer DONE, commits 5b8a32f + b726d81, HEAD b726d81; live probe enforcement only (no taxi rows seeded): 403/404 as expected; notes — TaxiDocument.ownerId is varchar vs uuid ids (join cast ::text, fixed live); surge has no market (Plan D); reviewer sonnet
Task 5 (A5): review ❌ 2 Important — (1) vendor-owned reviewDocument branch untested (→ add vendor cases + missing-owner case); (2) surge unscoped in backend (reviewer: plan-mandated; controller ruling corrected: my dispatch note 'leave unchanged' was a shortcut, the plan's fail-closed rule applies → tcpSurge throws Forbidden when scope is set until Plan D). Fix round 1/5 dispatched to implementer a8dfab6565837af52; re-review sonnet
Task 5 (A5): minor (deferred): getDrivers predicate does not upper-case countryCode (gateway normalises); Prettier reflow noise from lint-staged across four taxi files; processPayouts silently drops unmatched ids (pre-existing)
A6 pre-dispatch facts: GroceryAdminService repos storeRepo/itemRepo/orderRepo/flashDealRepo/zoneRepo/settingRepo; GroceryStore @Entity('grocery_stores') regionCode↔region_code; GroceryFlashDeal has storeId + relation property 'store' (ManyToOne, JoinColumn storeId); GroceryDeliveryZone.regionCode; grocery_categories has NO market column (global taxonomy → @GlobalEntity on reads, writes refuse locked admins — corrects the brief's Step 4 note); admin.grocery.products handler calls this.svc.getProducts(storeId, category, page, limit, undefined, {role:'ADMIN'}, approvalStatus) (GroceryService, not admin service); flash-deal approve/reject go through patterns approve_flash_deal {dealId, approvedBy} / reject_flash_deal {dealId, reason} to this.svc.approveFlashDeal(id, approvedBy)/rejectFlashDeal(id, dto); gateway rejectFlashDeal has no @Req today
A7 pre-dispatch facts: hotel.entity countryCode (+ nullable regionCode) — handlers admin_hotel_stats/admin_fraud_flags/admin_compliance/admin_revenue (no market → fail closed when scope), admin_list_hotels→getAllHotels(page,limit,status) (add countryCode predicate), admin_onboarding→getAllHotels(1,50,'PENDING_KYC') (same), admin_approve_hotel + admin.hotel.approve→approveHotel(id), admin_suspend_hotel + admin.hotel.suspend→suspendHotel(id, reason) (load + assert hotel.countryCode). restaurant.entity countryCode + regionCode — admin.restaurant.list→getAdminRestaurantList(d), approve/suspend→approveRestaurant/suspendRestaurant(id) (assert), admin.restaurant.cuisines→getCuisines() (global taxonomy). pharmacy-store.entity countryCode + regionCode — admin.pharmacy.stores→getAdminStoreList({status,page,limit}) (add country predicate), admin_list_pharmacy_stores (legacy alias), admin.pharmacy.categories→getCategories() (global). doctor: clinic.entity regionCode only; doctor.entity has NO market → admin.doctor.clinics→getClinics(city,specialty,page,limit) (add regionCode predicate), admin.doctor.doctors→getDoctors(specialty,page,limit) (fail closed when scope until Plan C5), admin.doctor.specialties (global). Gateway: 17+17+19+15 routes all need @Req + this.scopeOf( + countryCode: market, scope; commands without handlers keep 503 but carry scope.
Task 5 (A5): fix round 1/5 — implementer DONE: cab8782 (vendor doc cases + missing-owner case; tcpSurge refuses scope, probed 403 QA / 200 global); scoped re-review sonnet
Task 5 (A5): fix round 1/5 re-review — both addressed, no new breakage
Task 5 (A5): complete (commits 58a0bdb..cab8782, review clean after fix round 1; live probe: QA admin 403 on IN config/rates, surge 403 scoped / 200 global; no taxi rows seeded so lists proved empty-not-leaking → Tested)
Task 6 (A6): BASE 9f01518, brief task-6-brief.md (119 lines), implementer sonnet
Task 6 (A6): implementer DONE, commits 060ffab + ca3f75d, HEAD ca3f75d; live probe 403/200 across stores/orders/products/settings/categories/dashboard/reports; notes — getProducts region filter branches on privileged (admin sees non-approved stores in-market); updateDeliveryZone forces body.regionCode = scope; reviewer sonnet
Task 6 (A6): review ✅ spec compliant / Approved, 1 Important — no tests for the two disclosed deviations (getProducts privileged branch; updateDeliveryZone forces regionCode = scope); ⚠️ trailer resolved by controller. Fix round 1/5 dispatched to implementer aecf7513b8aa88700 (tests only); re-review sonnet
Task 6 (A6): minor (deferred): gateway actorId now defaults to the literal 'unknown' (consistent with admin-core, but a behaviour change); one gateway it() covers two refusals; updateCategory/deleteCategory refusal untested; three category handlers call this.scopeOf() only to satisfy the regression regex (documented)
Task 6 (A6): fix round 1/5 — implementer DONE: a6e891c (4 new spec cases: zone market forced/refused, products privileged vs public predicates); scoped re-review sonnet
Task 6 (A6): fix round 1/5 re-review — addressed, no new breakage
Task 6 (A6): complete (commits 9f01518..a6e891c, review clean after fix round 1; live probe QA admin 403 on IN stores/suspend/settings, QA-only stores/orders → Tested)
Task 7 (A7): BASE a0c60ec, brief task-7-brief.md (14 lines, prose) + ledger facts, implementer sonnet
Task 7 (A7): model escalated to opus before dispatch (four backends from a prose brief; A5/A6 on sonnet each needed a fix round)
Task 7 (A7): implementer DONE_WITH_CONCERNS, commits 3ec584b (gateway) + fd594ba (hotel) + 7847e25 (restaurant) + b23de47 (pharmacy) + 3787ae9 (doctor); 68 routes scoped; apps/api 430, hotel 21, restaurant 17, pharmacy 12, doctor 16 green; live probe: cross-market 403 ×4, taxonomy GET 200 / POST 403 ×4
Task 7 (A7): ruling — restaurant/pharmacy rows carry a legacy alpha-3 countryCode default (KEN/IND) never seeded; the platform market is ISO-2 regionCode → restaurant, pharmacy and clinic scope on regionCode; hotel keeps countryCode (seeded ISO-2). Fix sent to implementer before review
Task 7 (A7): minor (deferred): test/gateway-service-contract.spec.ts collects sent commands with a lowercase-only regex, so ~20 camelCase commands (incl. unimplemented admin.doctor.approveClinic) are invisible to it — widen in Plan C8 and baseline; Plan C4: admin_hotel_stats/admin_revenue fail closed (HotelReview has no market); Plan C5: admin.doctor.doctors fails closed (Doctor has no market)
A8 dry-run (controller, after A7's first commits): 355 routes parsed, 0 unscoped outside GLOBAL_ROUTES, 0 @GlobalEntity on non-GET → the A8 spec as written should pass; decorators/global-entity.decorator.ts already exists from A4 (A8 implementer must verify it matches, not recreate)
Task 7 (A7): fix before review DONE (eaa6b0b restaurant, 5b4853f pharmacy → regionCode); QA admin sees exactly the 4 Qatar restaurants; pharmacy seeds carry Mumbai district codes (MUM-*) not ISO-2 → empty for any locked admin (seed question, Plan C); HEAD 5b4853f; reviewer opus
Task 7 (A7): review ✅ approved, 1 Important — GET /admin/hotel/hotels/:id forwards scope to get_hotel → getHotelById(id) ignores it (cross-market hotel detail read). Ruling: fix now (allowed files extended to hotel.controller.ts + hotel.service.ts getHotelById). Fix round 1/5 dispatched to implementer a585331eb57326ff8; re-review sonnet
Task 7 (A7): minor (deferred): legacy admin_approve_hotel/admin_suspend_hotel read d.hotelId without requireId (first-row trap, not gateway-reachable); taxonomy writes send a provably-undefined scope; list predicates do not normalise case while assertInMarket does; gateway→service field mapping (d?.scope ?? d?.countryCode) untested at the controller seam for hotel/restaurant/pharmacy; pharmacy comment says legacy default 'IND' but entity default is 'KEN'; report claims :id reads send adminId (they send scope only — code is right)
Task 7 (A7): pre-existing for the final review / Plan C4: hotel admin.controller.ts exposes HTTP @Get('hotels')/@Put approve/suspend with no scope; pharmacy @Get('admin/stores') likewise; hotel @Get('bookings') and @Get('refunds') both return getAllHotels(...)
Task 7 (A7): fix round 1/5 — implementer DONE: 93a0ac9 (getHotelById asserts on cache-hit and query exits; probe IN hotel 403 for QA admin, cache exit proven); note for Plan C: admin.restaurant.get / admin.pharmacy.storeDetail / admin.doctor.clinicDetail / doctorDetail / admin.hotel.bookingDetail have no handler yet — implement with a record assert; scoped re-review sonnet
Task 7 (A7): fix round 1/5 re-review — addressed, no new breakage (refused reads do not populate the cache)
Task 7 (A7): complete (commits a0c60ec..93a0ac9, review clean after fix round 1; live probe cross-market 403 ×4, taxonomy GET 200 / POST 403 ×4, QA admin sees the 4 Qatar restaurants, IN hotel detail 403 → Tested)
Task 8 (A8): BASE 6eb15e2, brief task-8-brief.md (135 lines), implementer haiku
Task 8 (A8): implementer DONE, commit 777f2a4 (spec 3/3, suite 433/433, decorator verified identical); reviewer sonnet
Task 8 (A8): review ✅ approved, 2 Important plan-mandated (spec parser design, plan owner's own code): (1) last route bounded at EOF → trailing helpers forge 'scoped'; (2) multi-line route decorators invisible. Ruling: fix both — bound blocks at the next two-space member start / class brace; count declared decorators per file and fail on a mismatch. Fix round 1/5 dispatched to implementer a9ff125b7139702b1 with exact code; re-review sonnet
Task 8 (A8): minor (deferred): SCOPED is a text match (a string literal containing 'resolveMarket(' would count) — accepted trade-off given the naming constraint; backward decorator scan stops at a multi-line @ApiOperation (fails loud, inherited from route-exposure spec)
Task 8 (A8): controller correction — first fix-round code was wrong (MEMBER_START scan began right after the route decorator; the handler's own @ApiOperation lines ended the block → 334 false offenders in dry-run). Corrected: walk to the handler SIGNATURE line first, then scan for MEMBER_START (incl. constructor); dry-run 355 routes / 0 offenders / negative control passes; corrected code sent to implementer
Task 8 (A8): fix round 1/5 — implementer DONE: 7adf05a (signature-aware member boundary, declared-decorator count, negative control; 5/5, suite 435); scoped re-review sonnet
Task 8 (A8): fix round 1/5 re-review — both addressed; new Important in the fix diff: negative control duplicates the walk instead of calling the collector's logic (drift risk). Fix round 2/5 dispatched: shared handlerBlock(src, routeLine, nextRoute) used by collect() and the control
Regression check (controller, after A8): regional-isolation-authz.mjs first reported 6/36 — cause: the script reads the QA token from a hard-coded file under a PREVIOUS session's scratchpad (const TOK = '.../3df589cb-.../scratchpad/iso/qa-admin.token', stale since 2026-09-06) instead of logging in; with a fresh token in that file → 36/36. Promotion isolation holds. A9 must replace the file read with login('qa-admin@kartseek.com', ...)
Task 8 (A8): fix round 2/5 — implementer DONE: 79c17bf (shared handlerBlock; 5/5, suite 435); scoped re-review sonnet
Task 8 (A8): fix round 2/5 re-review — addressed, no new breakage
Task 8 (A8): complete (commits 6eb15e2..79c17bf, review clean after 2 fix rounds; spec 5/5: 355 routes, 0 unscoped, member-bounded blocks, decorator count, negative control; no live probe listed → Tested)
Task 9 (A9): BASE ca05a8e, brief task-9-brief.md (208 lines), implementer sonnet; live data: IN sellers 7, IN grocery stores 5, IN users 0, taxi drivers 0
Task 9 (A9): implementer DONE, commit 7b4501b; admin-scope-authz.mjs 19 passed / 0 failed; legacy authz 36/36 after replacing the stale token-file read with a login; script fix: products page/limit read from json.data (envelope); reviewer sonnet
Task 9 (A9): review ✅ approved (reviewer re-ran both scripts live: 19/19, 36/36), 1 Important plan-mandated — rows() fails open (empty/malformed list → vacuous pass); skipped conditional checks are silent. Fix round 1/5 dispatched to implementer ad547116f6f29dbab (listOf/onlyMarket/skip helpers, '○ skipped or empty' lines); re-review sonnet
Exit gate (controller, at 7b4501b + A9 fix pending): full suites green — apps/api 49 files/435 tests, marketplace 224, taxi 20, grocery 104, hotel 25, restaurant 18, pharmacy 12, doctor 16; builds running
Exit gate (controller): nest build --all exit 0; marketplace/taxi/grocery/hotel/restaurant/pharmacy/doctor builds exit 0
Task 9 (A9): fix round 1/5 — commit f1565a0 landed (listOf/onlyMarket/skip); scoped re-review sonnet
Task 9 (A9): fix round 1/5 re-review — addressed (reviewer re-ran live: 17 passed / 0 failed / 3 skipped or empty)
Task 9 (A9): complete (commits ca05a8e..f1565a0, review clean after fix round 1; live: admin-scope-authz 17/0/3, legacy authz 36/36 → Tested)
Final whole-branch review: package 3e6be86..464e5cf, reviewer fable; deferred list in deferred-findings.md
Final whole-branch review (fable, 3e6be86..464e5cf): Ready to merge = NO. Critical: (1) 12 finance/order mutations forward scope to refund/payout/order/commission services that ignore it; (2) 15 marketplace backend handlers drop scope (publish/unpublish/suspend/request-correction/feature/unfeature/featured add+remove/listing approve+reject/update_return_status/sponsored/qa-moderation/campaign/commission); (3) marketplace settings/SEO/notifications writes accept locked admins. Important: (4) read leaks — listings/pending, qa-moderation, sponsored, featured, customer-segments, notifications, compliance/countries, settings/seo reads, refunds, payouts, payouts/stats, commissions, rate-card, wallet/transactions; (5) id before body spread at 10 sites; (6) 31 inline refusals unlogged → shared refuseLockedAdmin (gateway) + refuseUnattributable (@app/common). Triage: fix-before-merge = the above + three 'admin' actor fallbacks + qa-moderation; follow-ups C1/C4/C5/C8/D/B as listed in the review. ONE fix wave dispatched (opus); then one scoped re-review
Final fix wave (opus): DONE — b2720b5 gateway (+libs/common, admin-service, script), f76b8c6 marketplace, 3232de4 hotel, 9bd7153 taxi, 7e087e8 doctor; apps/api 442, marketplace 236, hotel 25, taxi 20, doctor 16; builds exit 0; live proof 37/0/5, legacy 36/36 + e2e 67/67; extra: fixed a where()-resets-builder bug in the featured rail introduced mid-wave, and a '/*' inside a '//' comment in admin-service that hid 10 handlers from the contract spec's scan. Concern: trailers say 'Claude Opus 5' → implementer resumed to rewrite messages via filter-branch (trees unchanged). Then ONE scoped re-review (opus)
Final fix wave: trailers rewritten (filter-branch, trees unchanged) → 4912b2b, 4856c95, c7d341c, 90732e5, 543a8a5; scoped re-review opus on 464e5cf..543a8a5
Final fix wave re-review (opus, 464e5cf..543a8a5): findings 1,2,3,5,6,7,9 ADDRESSED; 4 and 8 partial; no new Critical/Important breakage; named risks clean; live proof reproduced 37/0/5; marketplace admin-scope spec 28 passed
Final: parked — GET /admin/marketplace/sponsored and sponsored-products neither predicated nor refused — ruling: returns a constant empty payload (dataAvailable:false), nothing to leak; Plan C implements sponsored products with scope from day one
Final: parked — no pharmacy/doctor decision probes in admin-scope-authz.mjs — ruling: those decision handlers do not exist (503 before any record is loaded), so a probe proves nothing until Plan C2/C5; the gateway side is pinned by A8
Final: follow-ups from the re-review — tcpAdminFeatureProduct builds { productId: d?.id, ...d } (id-before-spread one layer down; within-market retarget only, assert runs on the effective id) → C1; addFeaturedProduct/removeFeaturedProduct load the product twice → C1; test/gateway-service-contract.spec.ts stripComments is order-fragile for '/*' inside '//' → C8; seed data: pharmacy stores carry MUM-* district codes, restaurant/pharmacy alpha-3 countryCode defaults (KEN/IND), no IN users, no taxi drivers → C seed task
```

## Exit gate at 543a8a5

| Workspace           | Result                                                                                              |
| ------------------- | --------------------------------------------------------------------------------------------------- |
| apps/api            | 49 files, 442 tests green; `nest build --all` exit 0                                                |
| marketplace backend | 236 tests green; build exit 0                                                                       |
| grocery backend     | 104 tests green (untouched by the fix wave)                                                         |
| hotel backend       | 25 tests green; build exit 0                                                                        |
| taxi backend        | 20 tests green; build exit 0                                                                        |
| restaurant backend  | 18 tests green (untouched by the fix wave)                                                          |
| doctor backend      | 16 tests green; build exit 0                                                                        |
| pharmacy backend    | 12 tests green (untouched by the fix wave)                                                          |
| live proof          | `npm run verify:admin-scope` 37 passed / 0 failed / 5 skipped; legacy authz 36/36; legacy e2e 67/67 |
