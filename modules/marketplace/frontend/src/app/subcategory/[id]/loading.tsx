// The same skeleton for both listing routes, shaped exactly like the page it
// stands in for — see CatalogListingSkeleton.
export { CatalogListingSkeleton as default } from '../../components/catalog-listing';
