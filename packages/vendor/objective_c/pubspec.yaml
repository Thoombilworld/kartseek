# Copyright (c) 2024, the Dart project authors.  Please see the AUTHORS file
# for details. All rights reserved. Use of this source code is governed by a
# BSD-style license that can be found in the LICENSE file.
name: objective_c
description: 'A library to access Objective C from Flutter that acts as a support library for package:ffigen.'
version: 9.4.1
repository: https://github.com/dart-lang/native/tree/main/pkgs/objective_c
publish_to: none

topics:
  - interop
  - ffi
  - objective-c
  - codegen

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  collection: ^1.19.0
  ffi: ^2.1.0
  meta: ^1.15.0
  pub_semver: ^2.1.4

# ─────────────────────────────────────────────────────────────────────────
# DISABLED: The following sections are removed for the Windows workaround.
#
# The native build hook (hooks: build: hook/build.dart) crashes when the
# Windows user profile path contains spaces (e.g. "C:\Users\Your Name").
#
# Removed:
#   - hooks: build: hook/build.dart
#   - dependency_overrides (pointed to sibling packages in the dart-lang repo)
#   - code_assets, hooks, logging deps (only needed by the hook)
#
# NOTE: `collection` was also stripped here, but it is NOT hook-only —
# lib/src/ns_dictionary.dart and lib/src/ns_set.dart import it directly, so it
# has been restored above.
#   - dev_dependencies (not needed for override)
# ─────────────────────────────────────────────────────────────────────────
